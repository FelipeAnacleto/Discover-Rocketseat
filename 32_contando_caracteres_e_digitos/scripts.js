// Manipulando Strings e Números

// Contar quantos caracteres tem uma palavra e quantos digitos um número

let world = 'Paralelepipedo'
console.log(world.length);
let number = 1234;
console.log(String(number).length)